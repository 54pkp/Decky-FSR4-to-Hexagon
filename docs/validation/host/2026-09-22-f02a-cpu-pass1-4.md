# 2026-09-22 / F02a pass1–4 CPU参考

- 基线与目标：基于`014c3aec4d92820bddaf12c774e92694c75ce112`实现固定8x8x16 signed-int8输入的pass1–4 NumPy CPU参考，并用独立标量循环及认证simulator逐层对照；预声明最大误差0 LSB。
- 实现与结果：vector路径与不复用其卷积/舍入helper的raw-NPZ scalar oracle逐层一致，且认证simulator一致；p1–p4输出摘要依次为`f06fa187…a217b`、`17fdd33f…bc6f`、`435a882d…8a00`、`09b2b2f6…656a5`，每层观察到-128/127饱和。
- 实际角色：协调者型号/强度无独立回执，记`unknown`；实现worker和只读设计审计实际`gpt-5.6-sol / medium`；冻结diff由实际`gpt-6-astra / medium`审阅。

## 检查

| 命令 | 结果 | 边界 |
| --- | --- | --- |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest tests.fsr.test_cpu_passes_1_4 -v` | exit0；10/10通过 | 链式与四个独立direct fixture逐层0 LSB及隔离/边界负例 |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest discover -s tests\fsr -p test_*.py` | exit0；59/59通过 | FSR主机工具回归 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -p test_*.py` | exit0；342 tests，271 pass / 71 skip | NumPy F02a用例准确skip |
| `.venv\Scripts\python.exe -m py_compile tools\fsr\pass_manifest.py tools\fsr\cpu_passes_1_4.py tests\fsr\test_cpu_passes_1_4.py` | exit0 | 语法检查 |

- 审阅：Astra medium初审提出pre-tie舍入、scale元数据和direct输入依赖三个P2；补齐数学half-away、三路径完整tensor元数据、四个独立direct fixture及禁用完整链仍可运行p4的隔离回归后，独立专测10/10，最终`APPROVE`，无剩余阻断。
- 未测/限制：两条CPU路径共享accepted权重和simulator-derived算子语义；pinned simulator在合成pre-tie float32边界存在提前进位差异，因此与其0-LSB一致仅限本批固定链式/direct fixtures，不是所有舍入边界或官方operator/golden等价。固定小shape不是1080p或游戏输入；未实现pass5–13、ONNX、QNN/HTP、设备或游戏验证。
- 状态与下一步：F02a `complete`；下一叶F02b，远端确认本提交后才开始。
- Git交付：协调者已核对origin/main和`54pkp <67623881+54pkp@users.noreply.github.com>`；实际结果以Git历史为准。
