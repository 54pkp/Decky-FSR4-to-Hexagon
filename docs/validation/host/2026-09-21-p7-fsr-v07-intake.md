# 2026-09-21 / P7 FSR v07 材料接收与提取

- 目标与基准：基于 `0a50b07a9d38e5de96329721948d6aea185d93e3`，接收并锁定 FSR 4.0.2 `fsr4_model_v07_i8_quality` 五类输入，封装固定上游提取器；所有外部材料、环境和生成物只留在忽略目录。
- 来源：`Rolaand-Jayz/FSR-4.0.2-reference@c5e34b4b7128ffeb152f80cbdb6d715d577ec289`，通过 GitHub API/raw 取得；两次 filtered clone 因本机 GitHub 代理连接超时失败后未继续重试。提取器固定为 `puzzled-pancake/fsr4-hexagon@8c7a972ab70e5693828a856da71ce711232af463` 的 `build_weights.py`（15762 bytes，SHA-256 `73e5533bda45d17f84d2d81315c4152cb912d809a648f63fe06ec2e026f5d31f`）。同一固定提交的随包通知保存在忽略目录：`research/FSR-4.0.2-reference/LICENSE`（SHA-256 `58c6af3531a3534fb0c8c7810135ecc6ad1abbc139f4565c3f5a89f019559c6c`）、`research/fsr4-hexagon/LICENSE`（`2db153769c8a0f0a4b196be2e177de9e1acaaff62de7f51152ef067d0c12ef49`）和 `research/fsr4-hexagon/THIRD_PARTY.md`（`10821e7d107ac25275b955f30787ee8d22853a920565323b344ec4ee5a5c4aec`）。
- 产物：`tools/fsr/intake.py`、简短 README 和 9 项测试。入口检查显式路径、五个输入的固定大小/SHA-256/版本标记、断链/reparse、错误字节序和提取器哈希；在临时 SDK 布局运行复制的固定脚本，验证 NPZ/graph 语义后同父目录原子发布，拒绝覆盖，并写入来源、输入和输出哈希 receipt。
- 环境：Windows AMD64；忽略的 `local/venvs/fsr-extract` 使用 Python 3.12.14、NumPy 2.2.6 和基线 jsonschema 4.26.0。`FIDELITYFX_SDK_ROOT` 只注入提取子进程，系统 Python 3.9.13 未修改。

| 检查 | 结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m unittest discover -s tests\fsr -p "test_*.py" -v` | 退出 0；9/9 通过，含截断、哈希/版本、字节序、既有输出、提取失败、畸形产物和 symlink/reparse 拒绝 |
| `.venv\Scripts\python.exe -m py_compile tools\fsr\intake.py tests\fsr\test_intake.py` | 退出 0 |
| 固定五输入直接运行固定上游提取器 | 退出 0；全部上游 gate 通过，provider `weights_quality`、11 个 scale、31 个嵌入 i8 权重、13 个调用点及 pass13 重复数据交叉检查通过 |
| `intake.py` 对固定材料运行到新 `artifacts/p7-fsr-v07-accepted` | 退出 0；NPZ 216567 bytes / SHA-256 `9c644d42f421d6dfc2c13479dff84f1017c5457d00a14125796b3562944982fb`，100 arrays、0 object dtype；graph 43823 bytes / SHA-256 `8179991469fc0aae5fbc114c7e10f18ffb31555ff25351898b786198e70b2266`，11/58/13 结构检查通过 |
| 对同一输出重跑入口 | 退出 1；拒绝覆盖，NPZ、graph 和 receipt 三个哈希均不变 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py"` | 退出 0；183 项中 173 通过、10 skipped（1 项普通 symlink 权限；9 项需独立 ONNX CPU 环境） |
| `git diff --check` | 退出 0 |

- 审阅：Astra Medium 审阅冻结树，代码主路径无阻塞缺陷并复跑 focused 9/9；指出缺少随包通知，补齐上述同提交文件及哈希后批准本批。非阻塞残余为 graph 错误 marker/count 与公共 NPZ 语义失败尚无各自独立单测。
- 未测与边界：生成物不是随仓库分发的官方 FSR4 包，也没有官方 golden；本批只证明固定公开材料的离线接收、提取和内部交叉检查。QAIRT/QNN 转换、W8A8 encoding、HTP、GPU、Odin 3、游戏、画质、性能和功耗均 `not_run`。
- 下一批：P8 对本地提取结果做限定 CPU simulator/ONNX 参考对照；本轮到此停止。
