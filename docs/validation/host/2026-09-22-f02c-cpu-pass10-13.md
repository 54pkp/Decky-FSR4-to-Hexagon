# 2026-09-22 / F02c pass10–13 CPU参考

- 基线与目标：基于`884ac9516d2406350727bc81752283146ef109da`实现pass10–13 host CPU参考；每层走vector、独立循环scalar和认证simulator，固定p11的p2 skip且p13无skip。
- 实现与结果：p10–p12链式摘要为`1ab54077…`、`cb0b26ad…`、`7299e46d…`，三路径逐层0 LSB；p13摘要`56fd10a7…`，有限float16的uint16位模式逐元素相同、0 ULP。direct fixture分别独立验收，p11另有独立skip。
- 实际角色：协调者型号/强度无独立回执，记`unknown`；实现worker和只读设计审计实际`gpt-5.6-sol / medium`；冻结diff交实际`gpt-6-astra / medium`审阅。

## 检查

| 命令 | 结果 | 边界 |
| --- | --- | --- |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest tests.fsr.test_cpu_passes_10_13 -v` | exit0；8/8通过 | 四个独立direct、链式0 LSB/0 ULP、scalar路径隔离、CT相位/skip/layout/float16双舍入负例 |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest discover -s tests\fsr -p test_*.py` | exit0；75/75通过 | FSR主机工具回归 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -p test_*.py` | exit0；358 tests，271 pass / 87 skip | NumPy F02c用例准确skip |
| `local\venvs\fsr-extract\Scripts\python.exe -m py_compile tools\fsr\cpu_passes_10_13.py tests\fsr\test_cpu_passes_10_13.py` | exit0 | 语法检查 |

- 审阅：实际GPT-6 Astra medium发现scalar共用组合路径、来源合同可绕过、双舍入负例未走生产路径三项P2；拆分raw-key独立scalar、封闭来源/上游合同并令见证通过生产p13收尾后复审`APPROVE`，无剩余阻断。
- 未测/限制：p10–13 embedded权重没有独立`bin_raw__*`，scalar虽使用完整raw NPZ key与独立循环，布局仍共享P7 extractor；算子语义来自固定simulator且缺HLSLI。固定小shape不是1080p；不是官方golden、组合完整FSR4、ONNX、QNN/HTP、设备或游戏验证。
- 状态与下一步：F02c `complete`；下一叶F03，远端确认本提交后才开始。
- Git交付：协调者已核对origin/main和`54pkp <67623881+54pkp@users.noreply.github.com>`；实际结果以Git历史为准。
