# 2026-09-22 / F02b pass5–9 CPU参考

- 基线与目标：基于`361ffb2e7ec25a2780e3a7d90f6f8a5d615b852d`实现pass5–9 host CPU参考；每层同时走vector、独立raw-NPZ scalar oracle和认证simulator，预声明最大误差0 LSB。
- 实现与结果：覆盖FNB32、2x2下采样、FNB64 group2及FNB+CT2D+skip；p5–p9链式摘要依次为`c8e88153…`、`cbafbe8c…`、`96fe9396…`、`a1257b1a…`、`2727a711…`，三路径逐层0 LSB且观察到-128/127。
- 实际角色：协调者型号/强度无独立回执，记`unknown`；实现worker和只读设计审计实际`gpt-5.6-sol / medium`；冻结diff由实际`gpt-6-astra / medium`审阅。

## 检查

| 命令 | 结果 | 边界 |
| --- | --- | --- |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest tests.fsr.test_cpu_passes_5_9 -v` | exit0；8/8通过 | 五个独立direct、链式0 LSB、group2/bin raw/CT四相位/skip负例 |
| `local\venvs\fsr-extract\Scripts\python.exe -m unittest discover -s tests\fsr -p test_*.py` | exit0；67/67通过 | FSR主机工具回归 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -p test_*.py` | exit0；350 tests，271 pass / 79 skip | NumPy F02b用例准确skip |
| `local\venvs\fsr-extract\Scripts\python.exe -m py_compile tools\fsr\cpu_passes_5_9.py tests\fsr\test_cpu_passes_5_9.py` | exit0 | 语法检查 |

- 审阅：实际GPT-6 Astra medium发现合同元数据验收不完整的P2；补齐严格schema/comparison、JSON number类型、direct/skip全字段及三路径输出shape/dtype/scales/hash检查后复审`APPROVE`，无剩余阻断。
- 未测/限制：scalar从认证bin raw独立重建布局，但仍共享accepted P7与simulator-derived算子语义；simulator pre-tie限制延续。固定小shape不是1080p；不是官方golden、完整FSR4、ONNX、QNN/HTP、设备或游戏验证。
- 状态与下一步：F02b `complete`；下一叶F02c，远端确认本提交后才开始。
- Git交付：协调者已核对origin/main和`54pkp <67623881+54pkp@users.noreply.github.com>`；实际结果以Git历史为准。
